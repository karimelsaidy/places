class place {
    constructor(id,title,imageUri,address,notes) {
        this.id = id;
        this.title =title;
        this.imageUri = imageUri;
        this.address = address;
        this.notes = notes;
    }
}

export default place;